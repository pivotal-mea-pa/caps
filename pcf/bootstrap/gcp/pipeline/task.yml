---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: appbricks/automation-tools

inputs:
- name: automation

params:
  CONCOURSE_URL:
  CONCOURSE_USER:
  CONCOURSE_PASSWORD:
  GOOGLE_PROJECT:
  GOOGLE_CREDENTIALS_JSON:
  GOOGLE_REGION:
  BOOTSTRAP_STATE_BUCKET:
  BOOTSTRAP_STATE_PREFIX:
  PCF_PAS_RUNTIME_TYPE:
  PIVNET_API_TOKEN:
  OPSMAN_DOMAIN_OR_IP_ADDRESS:
  OPSMAN_CLIENT_ID:
  OPSMAN_CLIENT_SECRET:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:
  PRODUCTS:

run:
  path: automation/pcf/bootstrap/gcp/pipeline/task.sh
