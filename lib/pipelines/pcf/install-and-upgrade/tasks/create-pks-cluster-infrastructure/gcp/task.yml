---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: appbricks/automation-tools

inputs:
- name: automation
- name: automation-extensions

run:
  path: automation/lib/pipelines/pcf/install-and-upgrade/tasks/create-pks-cluster-infrastructure/gcp/task.sh

params:
  TRACE: Y
  
  GCP_RESOURCE_PREFIX:
  GCP_SERVICE_ACCOUNT_KEY:
  GOOGLE_PROJECT:
  GOOGLE_REGION:
  TERRAFORM_STATE_BUCKET:
  TEMPLATE_OVERRIDE_PATH:

  OPSMAN_HOST:
  OPSMAN_CLIENT_ID:
  OPSMAN_CLIENT_SECRET:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:
