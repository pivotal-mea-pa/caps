---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: appbricks/automation-tools

inputs:
- name: automation
- name: pivnet-opsmgr

run:
  path: automation/lib/pipelines/pcf/install-and-upgrade/tasks/upload-opsman/vsphere/task.sh

params:
  TRACE: Y

  GOVC_URL:
  GOVC_USERNAME:
  GOVC_PASSWORD:
  GOVC_INSECURE:

  VCENTER_DATACENTER:
  OPSMAN_VCENTER_CLUSTER:
  OPSMAN_VCENTER_DATASTORE:
  OPSMAN_VCENTER_NETWORK:
