---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: appbricks/automation-tools

inputs:
- name: automation
- name: pks-release

run:
  path: automation/lib/pipelines/pcf/install-and-upgrade/tasks/create-pks-loadbalancers/gcp/task.sh

params:
  TRACE: Y
  
  GCP_RESOURCE_PREFIX:
  GCP_PROJECT_ID:
  GCP_REGION:
  GCP_SERVICE_ACCOUNT_KEY:
  TERRAFORM_STATE_BUCKET:

  OPSMAN_HOST:
  OPSMAN_CLIENT_ID:
  OPSMAN_CLIENT_SECRET:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:

  PKS_API:
  PKS_USERNAME:
  PKS_PASSWORD: