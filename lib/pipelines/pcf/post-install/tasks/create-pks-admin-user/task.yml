---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: appbricks/automation-tools

inputs:
- name: automation
- name: terraform-output

run:
  path: automation/lib/pipelines/pcf/post-install/tasks/create-pks-admin-user/task.sh

params:
  TRACE: Y
  
  OPSMAN_HOST:
  OPSMAN_CLIENT_ID:
  OPSMAN_CLIENT_SECRET:
  OPSMAN_USERNAME:
  OPSMAN_PASSWORD:

  PKS_ADMIN_USERNAME:
  PKS_ADMIN_PASSWORD:
  PKS_ADMIN_EMAIL:
